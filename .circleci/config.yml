version: 2.1

jobs:
  test:
    docker:
      - image: python:latest
    steps:
      - run: echo "testing..."

  deploy:
    docker:
      - image: python:latest
    steps:
      - run: echo "deploying...."

workflows:
  build_deploy:
    triggers:
      - schedule:
          filters:
            branches:
              only:
                - prod
                - uat
    jobs:
      - test
      - deploy
